export const users = "/api/users/{user_id}{?brand}";

export const user_memberships = "/api/users/{user_id}/memberships{?brand}";

export const current_user = "/api/users/current{?brand}";

export const user_exists = "/api/users/exists";
