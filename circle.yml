machine:
  node:
    version:
      6.11.1

dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn

test:
  override:
    - yarn test

deployment:
  staging:
    branch: master
    heroku:
      appname: test-le-svc-discovery
